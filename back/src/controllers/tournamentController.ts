import AbstractController from "./abstractController";

class TournamentController extends AbstractController {
    // Exemple: this.ok(res, tournaments), this.findById(), etc.
}

export default new TournamentController();
