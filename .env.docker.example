# Copiez ce fichier en .env pour personnaliser (optionnel)
# Par défaut, docker compose up fonctionne sans ce fichier

PORT=3000
DB_PORT=3306
DB_USER=babyfoot
DB_PASSWORD=babyfoot
DB_NAME=babyfoot_clementine
MYSQL_ROOT_PASSWORD=rootpassword
JWT_SECRET=change-me-in-production
